{
  "name": "@event-roster/workers-bcrypt-capability",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "check": "tsc --noEmit && tsc --noEmit -p tsconfig.scripts.json",
    "generate:dummy": "tsx scripts/generate-dummy.mts"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "hono": "^4.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.6",
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.45.0"
  }
}
